doctype html
html
    head
        title Login Page
        link(rel="stylesheet", href="stylesheets/login.css")

    body
        h2#loginH2 BookMate Login
        form(id="form", action="/admit", method="post")
            label.longinLabel
                span 아이디
                input(type="text", name="login")
            br
            label.longinLabel
                span 비밀번호
                input(type="password", name="password")
            br
            button#loginBtn(type="submit") 로그인